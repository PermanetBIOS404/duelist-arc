# Progress — 2026-05-06

Focus: prepare Duelist ARC for an early public demo launch with **CPU-first gameplay**, clearer presentation, and safe scaffolding for future expansion.

## Shipped (CPU dueling / demo reliability)

- **Browser CPU duel is the primary demo path** (CoreIntegrator session + SSE).
  - CPU-side prompts (`receiver=1`) auto-respond with safe defaults to avoid stalls.
  - Auto-processing pauses when human input is required (`receiver=0`) and resumes after a response.
  - Added a lightweight stall watchdog: warns on slow output and stops auto-process with recovery guidance if the core goes silent.
- Duel tab now has a prominent **“Playable Now”** banner with:
  - **Duel a CPU (browser)** primary start button + readiness status (`/duel/session/health`).
  - Demo-safe auto-load of a sample deck if the current deck is invalid (<40 main) before starting.
- Legacy WindBot/EDOPro “room join” flow is clearly labeled **WindBot CPU (Legacy)** and hidden behind Advanced in demo mode.

## Shipped (Banlists / parity UX)

- Added quick-pick **banlist chips** (EDOPro-style naming conventions) under the legacy WindBot banlist input:
  - Latest TCG / Latest OCG / GOAT / Edison / Master Duel (when present).

## Shipped (Deck import UX)

- Duel tab **Deck Test (Instant)** now includes **Paste list** for quick deck imports from plain text (uses the existing list parser).

## Shipped (Expanding Realms / platform scaffolding)

- Added a new demo landing section: **Expanding Realms** (polished realm tiles + status badges).
  - Yu-Gi-Oh: **Playable Demo Available**
  - Other realms: Coming Soon / In Development / Planned Expansion
  - Includes atmospheric modal “Preview Vision” panels (no fake systems).
- Added a **Roadmap** section with phases:
  - CPU Dueling → Online Multiplayer → Profiles & Cosmetics → Cross‑TCG Expansion → Emulator Hub
- Visual polish: realm-specific accents, hover glow/lift, icon chips, shimmer (respects reduced motion).

## Shipped (Always-on deployment skeleton)

- Improved `docker-compose.duel.yml` for always-on service shape:
  - `restart: unless-stopped`
  - healthchecks for `edopro` and `duelist-arc`, with `depends_on` waiting for `edopro` healthy.
  - optional Docker target support so Duelist ARC can bundle CoreIntegrator via a `bundled` build target.
- Updated `server/DEPLOY_DUEL_STACK.md` with recommended env pins/targets.

## Shipped (Launch-safe account guardrails)

- Added reserved-username validation on registration (blocks admin/mod/staff/system/dev/etc. obfuscations).
  - User-facing message: **“That username is reserved. Please choose another name.”**

## Shipped (Account email skeleton — for tomorrow’s password reset)

- Added `users.email` storage skeleton (DB migration + endpoints) and a small UI block to save email:
  - `GET /auth/profile`
  - `POST /auth/email`
  - UI: **Account email (skeleton)** visible when signed in
- Password reset codes via email: **NOT implemented yet** (intentionally deferred).

## Key files touched today

- Frontend: `src/index.html`, `src/js/main.js`, `src/css/style.css`
- Server: `server/src/routes/auth.js`, `server/src/db.js`, `server/db/schema.sql`
- Deploy: `docker-compose.duel.yml`, `docker/DuelistArc.Dockerfile`, `server/DEPLOY_DUEL_STACK.md`

## Next (morning)

- Implement password reset flow (request code → verify → set new password), using the new `users.email` skeleton.
- Continue CPU duel robustness: better prompt decoding coverage + clearer action affordances.
