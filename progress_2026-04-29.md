# Progress — 2026-04-29

Focus: turn the localhost prototype into a **presentation-grade demo** that feels professionally built, with a clear “online dueling coming soon” message, instant deck interaction, and deploy-hardening for a public Cloudflare-hosted demo.

## Shipped (UI / Demo polish)

- Demo-first experience:
  - Added a dedicated **Demo landing** view (hero, CTAs, start-here steps).
  - Added **Demo mode** toggle (streamlined UI) + persistent preference.
  - Added a **Demo tour** modal (guided onboarding) + “show once” behavior.
  - Added a **Developer page** (`/dev.html`) describing the project vision, roadmap, and tournament concept + FAQ.
- “Do something immediately” gameplay:
  - Added **Deck Test (Instant)** in the Duel tab: upload a deck → shuffle + draw 5 → show hand.
  - Added **Sample decks** (one-click load + auto draw 5).
- Trust-building demo UX:
  - Added a **status pill row** (server / CPU duel config / dataset quality).
  - Added **CPU Duel Readiness** (EDOPro/WindBot/banlists checks + suggested fixes).
  - Added **Settings drawer** (demo mode, theme, tour, reset, feedback).
  - Added **Feedback** (copy template) and **Diagnostics** (copy full runtime details).
  - Added **Screenshots section** (themed SVG “shots” for the landing page).
  - Added **footers** with a canonical “Start demo” link.

## Shipped (Server / Deploy hardening)

- Server listen improvements:
  - Added `HOST` env support and better listen error logging.
- Security + ops headers:
  - Added basic security headers and `trust proxy`.
  - Added caching headers for static assets (CSS/JS/images).
- Public-demo quality endpoints:
  - Added `/robots.txt` and `/sitemap.xml`.
  - Added a friendly `404.html` + server-side HTML 404 fallback.
- Duel stack readiness endpoints:
  - Added `GET /cpu-duel/edopro/health` for WindBot configuration checks.
- Docker demo improvements:
  - Ensured `HOST=0.0.0.0`.
  - Added demo-hardening envs: `DUEL_LOCAL_ENABLED=false`, `DUEL_EXPOSE_JOIN_INFO=false`, `DUEL_EXPOSE_ROOM_PASSWORD=false`.
  - Persisted server data by mounting `./server/data:/app/server/data` and setting `DB_PATH=/app/server/data/app.db`.

## Notes / Known constraints

- This sandbox environment cannot bind to localhost ports (EPERM), so server runtime was validated by code inspection and syntax checks, but **not** by actually listening on `:8787` here.
- CPU duels depend on EDOPro-server-ts + WindBot being configured on the deployment host; the demo now shows clean readiness + diagnostics even if they are missing.

## Key demo URLs

- Canonical “first impression” link:
  - `/?demo=1&view=demo&tour=1&sample=classic_demo&draw=5`
- Developer page:
  - `/dev.html`

